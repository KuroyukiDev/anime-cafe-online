<!DOCTYPE html>
<html>
<head>
   <title>{{pageTitle}} - Anime Cafe</title>
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1">

   <link rel="icon" type="image/png" href="/public/android-icon-192x192.png">
   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
   <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css">
   <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
   <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

   <link rel="stylesheet" href="css/styles.css">

</head>
<body>

<nav class="navbar navbar-inverse navbar-color-theme">
   <div class="container-fluid">
      <div class="navbar-header">
         <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
         </button>
         <a class="navbar-brand logo-img" href="/">Anime Cafe</a>
      </div>
      <div class="collapse navbar-collapse" id="myNavbar">
         <ul class="nav navbar-nav text-center">
            <li><a href="/">Home</a></li>
            <li class="active"><a href="/dubbed">Dubbed</a></li>
            <li><a href="/donate">Donate</a></li>
         </ul>
      </div>
   </div>
</nav>

<div class="container-fluid">

   <div class="lead">
      <h1 class="text-center">{{pageTitle}}</h1>
   </div>

   <div class="row">
      <div class="col-sm-6">
         <div class="list-group">
            <a href="{{ asobini.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ asobini.banner }}"
                     alt="Cover image banner for {{ asobini.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ asobini.name }}</h4>
            </a>

            <a href="{{ barakamon.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ barakamon.banner }}"
                     alt="Cover image banner for {{ barakamon.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ barakamon.name }}</h4>
            </a>

            <a href="{{ danmachi.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ danmachi.banner }}"
                     alt="Cover image banner for {{ danmachi.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ danmachi.name }}</h4>
            </a>

            <a href="{{ withmysmartphone.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ withmysmartphone.banner }}"
                     alt="Cover image banner for {{ withmysmartphone.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ withmysmartphone.name }}</h4>
            </a>

            <a href="{{ newgame.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ newgame.banner }}"
                     alt="Cover image banner for {{ newgame.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ newgame.name }}</h4>
            </a>

         </div>
      </div>

      <div class="col-sm-6">
         <div class="list-group">

            <a href="{{ mikagura.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ mikagura.banner }}"
                     alt="Cover image banner for {{ mikagura.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ mikagura.name }}</h4>
            </a>

            <a href="{{ gamers.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ gamers.banner }}"
                     alt="Cover image banner for {{ gamers.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ gamers.name }}</h4>
            </a>

            <a href="{{ haganai.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ haganai.banner }}"
                     alt="Cover image banner for {{ haganai.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ haganai.name }}</h4>
            </a>

            <a href="{{ monmusu.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ monmusu.banner }}"
                     alt="Cover image banner for {{ monmusu.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ monmusu.name }}</h4>
            </a>

            <a href="{{ demichankataritai.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ demichankataritai.banner }}"
                     alt="Cover image banner for {{ demichankataritai.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ demichankataritai.name }}</h4>
            </a>

            <a href="{{ souleaternot.id }}"
               class="list-group-item">
               <img
                     class="img-responsive"
                     style="height: 25vh; width: 100%;"
                     src="{{ souleaternot.banner }}"
                     alt="Cover image banner for {{ souleaternot.name }} with the name of the series and some characters from the series"
               />
               <h4 class="text-center">{{ souleaternot.name }}</h4>
            </a>

         </div>
      </div>
   </div>

</div>

</body>
</html>
